# PRECAUTION: avoid production dependencies that aren't in development

-r base.txt

gevent==24.2.1  # http://www.gevent.org/
gunicorn==22.0.0 # https://github.com/benoitc/gunicorn
psycopg2-binary==2.9.9  # https://github.com/psycopg/psycopg2
sentry-sdk[django]==2.5.1  # UPGRADE de 1.43.0 - extra [django] explícito, alinhado ao core

# Django
# ------------------------------------------------------------------------------
django-anymail==10.3  # https://github.com/anymail/django-anymail
setuptools==80.10.2 # not directly required, pinned by Snyk to avoid a vulnerability

# Elastic-APM  # https://pypi.org/project/elastic-apm/
# ------------------------------------------------------------------------------
elastic-apm==6.21.4.post8347027212  # PENDENTE — fora do escopo deste bump (decisão de remoção em aberto)